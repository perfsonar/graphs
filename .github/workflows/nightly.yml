name: nightly
on: 
  push :
    branches:
      - github-actions
jobs:
  setup-containers:
    strategy:
      matrix:
         os: [ 'el8' ]
      
    runs-on: ubuntu-latest
   
    container: 
      image: ghcr.io/perfsonar/unibuild/${{ matrix.os }}:latest
        
    steps:
       - name: Check out Repo
         uses: actions/checkout@v3
 
